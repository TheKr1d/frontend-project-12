{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend && mkdir -p build && cp -r frontend/dist/* build/",
    "start": "npx start-server -s ./build",
    "postinstall": "cd frontend && npm ci"
  }
}